# Roadmap to do Linux in 2026

## 1. Linux

-  File system hierachy (/, /var, /etc, /proc)
-  File type, Inodes, Symbolic vs hard links
-  User, groups, Permissions, umask
-  sudo internals & Security implications
-  Process lifecycle (fork, exec, signals)
-  systemd:
      -  units, targets, dependencies
      -  service fauiler, restart loops
- Logs:
      -  journalclts deep usage
      -  app logs vs system logs
- Disk:
      -  mount points, fstab awarness
      -  disk pressure & inode exhaustion
  - Memory & CPU:
      -  free , vmstat basics
      -  CPU load vs CPU usage
  - SSH
      -  key-based auth
      -  port forward use cases
  - Common faliur
      -  service is not starting
      -  permision denied
      -  disk full
      -  high load
   
 
  
